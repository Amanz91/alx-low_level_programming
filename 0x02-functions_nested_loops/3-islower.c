#include "main.h"
/**
 * _islower - program to check for lowercase character
 * Return: 1 or 0
 */
int _islower(int c)
{
	int r;
	int i = 1;
	int j = 0;

	if (r = 'a'; r <= 'z'; r++)
	{
	_putchar(i);
	}
	else
	_putchar(j);
	}
}
