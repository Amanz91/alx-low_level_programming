#include "main.h"
/**
 * _islower - program to check for lowercase character
 * Return: 1 or 0
 */
int _islower(int c)
{
	int c;
	int i = 1;
	int j = 0;

	if (c = 'a'; c <= 'z'; c++)
	{
	_putchar(i);
	}
	else
	_putchar(j);
	}
}
