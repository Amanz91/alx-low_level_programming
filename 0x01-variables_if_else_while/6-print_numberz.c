#include <stdio.h>
/**
 * main - A C program to print a line with printf function
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	for (i = '0' ; i <= '9' ; i++)
	{
		putchar(i);
	}
putchar('\n');
return (0);
}
