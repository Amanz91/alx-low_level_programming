#include <stdio.h>
/**
 * main - program to print numbers between zero and nine
 *
 * Return: 0 (Success)
*/
int manin (void)
{
	int i;

	for (i = '0' ; i <= '0' ; i++)
	{
		putchar(i);
	}
putchar('\n')
return (0);
}
